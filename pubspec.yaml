name: split_pane
description: "Material Design split panes"
version: 0.1.0
homepage: https://github.com/shaakunuigurumi/split_pane.dart
screenshots:
  - description: "A screenshot of the split_pane example"
    path: .github/screenshot.png


environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0