name: split_pane
description: "A Flutter package for Material Design's split-pane layouts"
version: 0.2.0
homepage: https://github.com/shaakunuigurumi/split_pane
issue_tracker: https://github.com/shaakunuigurumi/split_pane/issues
screenshots:
  - description: "A screenshot of the split_pane example"
    path: screenshots/example.png

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0